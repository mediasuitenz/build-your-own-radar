apiVersion: v1
kind: Service
metadata:
  name: {{ include "techradar.fullname" . }}
  labels:
    {{- include "techradar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "techradar.selectorLabels" . | nindent 4 }}
